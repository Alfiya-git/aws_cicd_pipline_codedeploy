version: 0.2

phases:
  build:
    commands:
      - echo "Creating AMI backup..."
      - |
        aws ec2 create-image \
          --instance-id i-027962a43049bf313 \
          --name "backup-CodeDeploy" \
          --tag-specifications "ResourceType=image,Tags=[{Key=name,Value=CodeDeploy}]"
        

# artifacts:
#   files: '**/*'
